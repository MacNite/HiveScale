; PlatformIO compiles every .cpp under src/ automatically, so the split needs
; no manual file list. include/ is on the global header search path.
;
; This firmware targets Arduino-ESP32 3.x (ESP-IDF 5.x), which the current
; `espressif32` platform resolves to. The mic capture path uses the new
; channel-based I2S "standard" driver (driver/i2s_std.h); it will NOT build
; against the old Arduino-ESP32 2.x legacy I2S driver. For a reproducible build
; you can pin a specific 3.x platform release here, e.g.:
;   platform = espressif32 @ <known-good-3.x-version>
[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
board_build.partitions = min_spiffs.csv
; Name the compiled artifacts after FIRMWARE_VERSION (src/globals.cpp),
; e.g. hivescale_0.9.2.bin instead of firmware.bin.
extra_scripts = pre:rename_firmware.py
build_flags =
    -std=gnu++17
    -Os
    -DCORE_DEBUG_LEVEL=1
lib_deps =
    bogde/HX711
    paulstoffregen/OneWire
    milesburton/DallasTemperature
    adafruit/Adafruit SHT4x Library
    adafruit/RTClib
    bblanchon/ArduinoJson
; Required because secrets.h sets ENABLE_INMP441_MICS = 1
    kosme/arduinoFFT@^2.0.4
; Optional features — uncomment only if the matching flag is set in secrets.h:
; adafruit/Adafruit INA219                                  ; ENABLE_INA219_SOLAR
; sparkfun/SparkFun MAX1704x Fuel Gauge Arduino Library     ; ENABLE_MAX17048_BATTERY